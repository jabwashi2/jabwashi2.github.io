/* for creating/showing project details components:
* - each object has isCreated bool (starts false)
* if false, create component
* x/close button hides component
* if created, show again
*/

// project mini button onclick function: creates detail component or shows component
function showDetails(projectName) {
    // look through objects to find mathing title with projectName
    // if found, see if isCreated == true
    // if not true, create component
    // if true, find component and set visibility to show
}